{
  "name": "io.oreon.accounting.server",
  "version": "0.0.1",
  "scripts": {
    "type-check": "tsc",
    "build": "babel src -x .js,.ts -d dist --copy-files",
    "build:watch": "yarn build -w",
    "start": "node startup-scripts/start.js",
    "deploy": "echo 'to implement with netlify'",
    "lint": "eslint --fix --ext .js,.ts ."
  },
  "engines": {
    "node": "12"
  },
  "main": "dist/index.js",
  "dependencies": {
    "graphql": "^15.4.0",
    "apollo-server-lambda": "^2.19.1",
    "dotenv": "^8.2.0",
    "firebase-admin": "^9.3.0",
    "googleapis": "^59.0.0"
  },
  "devDependencies": {
    "concurrently": "^5.3.0"
  }
}
